import { naoImplementado } from './stubs.js';

const lancar = naoImplementado('alertas');

export const listarAlertas = lancar;
export const obterAlerta = lancar;
export const guardarAlerta = lancar;
export const atualizarAlerta = lancar;
export const eliminarAlerta = lancar;
